<!DOCTYPE html>
<html lang="en">
    <head>
  <link rel="icon" type="image/png" href="/logo2.png">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Anyone</title>
        <link rel="stylesheet" href="/css/homePage.css">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
        <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
    </head>
    <body>
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/logo.png" alt="" style="width:40px;background-color: #ffffff;" class="rounded-pill">
                Anyone
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                ☰
            </button>
            <div class="collapse navbar-collapse" id="navbarContent">
                
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link text-white" href="/profile/">Profile</a></li>
                <!-- <li class="nav-item"><a class="nav-link text-white" href="#">Settings</a></li> -->
                </ul>
                <form class="Searchorm">
                    <input class="form-control" type="search" placeholder="Search..." aria-label="Search">
                    <button class="searchButton" type="submit">
                        <i class='fas fa-search	'></i>
                    </button>
                </form>
            </div>
            </div>
        </nav>

        <!-- Main Container -->
        <div class="container mt-4">
            <div class="row justify-content-center">
            <div class="col-md-6">

                <!-- Post Box -->
                <div class="post-box">
                    
                    <!-- Posts Feed -->
                    <div id="feed">
                    
                    <!-- posts here -->
                    <div class="post">
                        <a style="all: unset;cursor: pointer;" href="/profile/<%= user_username %>">
                            <div class="publisher" style="text-align: start;">
                                <img src="/images/avatars/d (<%= user_image_id || "0" %>).png" alt="" style="width:30px;background-color: #ffffff;" class="rounded-pill">
                                <%= user_name || "username not found" %> <span class="verified"><i class='far fa-check-circle'></i></span>
                            </div>
                        </a>
                        <!-- <small>2h ago</small> -->
                        <p><%= body %></p>
                        <hr>
                        <% if(thereaccount) { %>
                        <form class="postFormFather"  method="post" action="/toolpostacomment">
                            <input class="postform form-control" name="bodyComment" type="search" placeholder="Add Comment Here!" aria-label="Search">
                            <input class="" value="<%=postid  %>" name="postId" style="display: none;">
                            <input value="<%= mainuser.profileImage %>" name="publisherImageid" style="display: none;">
                            <input value="<%= mainuser._id %>" name="publisherid" style="display: none;">
                            <input value="<%= mainuser.username %>" name="publisherusername" style="display: none;">
                            <input value="<%= mainuser.name %>" name="publishername" style="display: none;">
                            <button class="postFormButton" type="submit">
                                <i class='fa fa-comment	'></i>
                            </button>
                        </form>
                        <hr>
                        <% } %>
                        <% if(commentslist.length != 0) { %>
                            <h4 style="font-weight: 600;font-family: Arial, Helvetica, sans-serif;">All Comments</h4>
                        <% } else { %>

                            <h5 style="font-weight: 600;font-family: Arial, Helvetica, sans-serif;">There is no Comments</h5>
                        <% } %>
                        <% for( let index = 0; index < commentslist.length; index++ ) { %>
                        
                            <div class="postFormFather">
                                <div class="postform2 form-control">
                                    <a style="all: unset;cursor: pointer;" href="/profile/<%= commentslist[index].publisherusername %>">
                                        <div class="publisher" style="text-align: start;">
                                            <img src="/images/avatars/d (<%= commentslist[index].publisherImageid || "0" %>).png" alt="" style="width:30px;background-color: #ffffff;" class="rounded-pill">
                                            <%= commentslist[index].publishername || "username not found" %> <span class="verified"><i class='far fa-check-circle'></i></span>
                                        </div>
                                    </a>
                                    <hr>
                                    <span>
                                        <%= commentslist[index].body || "body not found"%>
                                    </span>
                                </div>
                            </div>
                        <% } %>
                        
                    </div>
                </div>
            </div>
            </div>
        </div>
    </body>
</html>